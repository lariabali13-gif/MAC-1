<UserControl x:Class="MAC_1.Views.DashboardSection"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:fa="http://schemas.fontawesome.io/icons/"
             xmlns:conv="clr-namespace:MAC_1.Converters"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="900">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <conv:FileIconConverter x:Key="FileIconConverter"/>
        <Storyboard x:Key="CardHoverIn">
            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleX)" To="1.02" Duration="0:0:0.15"/>
            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleY)" To="1.02" Duration="0:0:0.15"/>
        </Storyboard>
        <Storyboard x:Key="CardHoverOut">
            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleX)" To="1" Duration="0:0:0.15"/>
            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleY)" To="1" Duration="0:0:0.15"/>
        </Storyboard>
        <Style x:Key="SlimProgress" TargetType="ProgressBar">
            <Setter Property="Height" Value="4"/>
            <Setter Property="Background" Value="#E5E7EB"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Grid>
                            <Border Background="{TemplateBinding Background}" CornerRadius="2"/>
                            <Border x:Name="PART_Track" CornerRadius="2"/>
                            <Border x:Name="PART_Indicator" HorizontalAlignment="Left" CornerRadius="2">
                                <Border.Background>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                        <GradientStop Color="#3B82F6" Offset="0"/>
                                        <GradientStop Color="#60A5FA" Offset="1"/>
                                    </LinearGradientBrush>
                                </Border.Background>
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Border Background="#F0F2F5">
        <ScrollViewer VerticalScrollBarVisibility="Hidden" HorizontalScrollBarVisibility="Disabled">
            <StackPanel Margin="28,20,28,20">
                <!-- HEADER -->
                <Grid Margin="0,0,0,20">
                    <StackPanel VerticalAlignment="Center">
                        <StackPanel Orientation="Horizontal">
                            <Border Width="32" Height="32" CornerRadius="8" Margin="0,0,10,0">
                                <Border.Background>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                        <GradientStop Color="#3B82F6" Offset="0"/>
                                        <GradientStop Color="#1D4ED8" Offset="1"/>
                                    </LinearGradientBrush>
                                </Border.Background>
                                <fa:ImageAwesome Icon="Tachometer" Width="14" Foreground="White" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                            </Border>
                            <TextBlock Text="Dashboard" FontSize="20" FontWeight="Bold" Foreground="#0F172A" VerticalAlignment="Center"/>
                        </StackPanel>
                    </StackPanel>
                    <Border HorizontalAlignment="Right" VerticalAlignment="Center" Cursor="Hand">
                        <Border Background="White" CornerRadius="16" Padding="10,5" BorderBrush="#E2E8F0" BorderThickness="1">
                            <StackPanel Orientation="Horizontal">
                                <Ellipse Width="7" Height="7" Fill="#22C55E" Margin="0,0,6,0" VerticalAlignment="Center">
                                    <Ellipse.Triggers>
                                        <EventTrigger RoutedEvent="Loaded">
                                            <BeginStoryboard>
                                                <Storyboard RepeatBehavior="Forever">
                                                    <DoubleAnimation Storyboard.TargetProperty="Opacity" From="1" To="0.3" Duration="0:0:0.8" AutoReverse="True"/>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </EventTrigger>
                                    </Ellipse.Triggers>
                                </Ellipse>
                                <TextBlock Text="Live" FontSize="11" FontWeight="SemiBold" Foreground="#16A34A" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                    </Border>
                </Grid>
                <!-- STAT CARDS -->
                <Grid Margin="0,0,0,16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Border Grid.Column="0" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="12" Padding="12,10" Cursor="Hand" RenderTransformOrigin="0.5,0.5">
                        <Border.RenderTransform><ScaleTransform ScaleX="1" ScaleY="1"/></Border.RenderTransform>
                        <Border.Effect><DropShadowEffect BlurRadius="8" ShadowDepth="2" Opacity="0.05"/></Border.Effect>
                        <Border.Triggers>
                            <EventTrigger RoutedEvent="Border.MouseEnter"><BeginStoryboard Storyboard="{StaticResource CardHoverIn}"/></EventTrigger>
                            <EventTrigger RoutedEvent="Border.MouseLeave"><BeginStoryboard Storyboard="{StaticResource CardHoverOut}"/></EventTrigger>
                        </Border.Triggers>
                        <StackPanel>
                            <Border Width="26" Height="26" CornerRadius="6" Background="#EFF6FF" Margin="0,0,0,6">
                                <fa:ImageAwesome Icon="Download" Width="11" Foreground="#3B82F6" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                            </Border>
                            <TextBlock Text="{Binding TotalDownloads}" FontSize="18" FontWeight="Bold" Foreground="#0F172A"/>
                            <TextBlock Text="Downloads" FontSize="10" Foreground="#64748B"/>
                        </StackPanel>
                    </Border>
                    <Border Grid.Column="2" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="12" Padding="12,10" Cursor="Hand" RenderTransformOrigin="0.5,0.5">
                        <Border.RenderTransform><ScaleTransform ScaleX="1" ScaleY="1"/></Border.RenderTransform>
                        <Border.Effect><DropShadowEffect BlurRadius="8" ShadowDepth="2" Opacity="0.05"/></Border.Effect>
                        <Border.Triggers>
                            <EventTrigger RoutedEvent="Border.MouseEnter"><BeginStoryboard Storyboard="{StaticResource CardHoverIn}"/></EventTrigger>
                            <EventTrigger RoutedEvent="Border.MouseLeave"><BeginStoryboard Storyboard="{StaticResource CardHoverOut}"/></EventTrigger>
                        </Border.Triggers>
                        <StackPanel>
                            <Border Width="26" Height="26" CornerRadius="6" Background="#F0FDF4" Margin="0,0,0,6">
                                <fa:ImageAwesome Icon="CheckCircle" Width="11" Foreground="#22C55E" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                            </Border>
                            <TextBlock Text="{Binding CompletedCount}" FontSize="18" FontWeight="Bold" Foreground="#0F172A"/>
                            <TextBlock Text="Completed" FontSize="10" Foreground="#64748B"/>
                        </StackPanel>
                    </Border>
                    <Border Grid.Column="4" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="12" Padding="12,10" Cursor="Hand" RenderTransformOrigin="0.5,0.5">
                        <Border.RenderTransform><ScaleTransform ScaleX="1" ScaleY="1"/></Border.RenderTransform>
                        <Border.Effect><DropShadowEffect BlurRadius="8" ShadowDepth="2" Opacity="0.05"/></Border.Effect>
                        <Border.Triggers>
                            <EventTrigger RoutedEvent="Border.MouseEnter"><BeginStoryboard Storyboard="{StaticResource CardHoverIn}"/></EventTrigger>
                            <EventTrigger RoutedEvent="Border.MouseLeave"><BeginStoryboard Storyboard="{StaticResource CardHoverOut}"/></EventTrigger>
                        </Border.Triggers>
                        <StackPanel>
                            <Border Width="26" Height="26" CornerRadius="6" Background="#FEF2F2" Margin="0,0,0,6">
                                <fa:ImageAwesome Icon="TimesCircle" Width="11" Foreground="#EF4444" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                            </Border>
                            <TextBlock Text="{Binding FailedCount}" FontSize="18" FontWeight="Bold" Foreground="#0F172A"/>
                            <TextBlock Text="Failed" FontSize="10" Foreground="#64748B"/>
                        </StackPanel>
                    </Border>
                    <Border Grid.Column="6" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="12" Padding="12,10" Cursor="Hand" RenderTransformOrigin="0.5,0.5">
                        <Border.RenderTransform><ScaleTransform ScaleX="1" ScaleY="1"/></Border.RenderTransform>
                        <Border.Effect><DropShadowEffect BlurRadius="8" ShadowDepth="2" Opacity="0.05"/></Border.Effect>
                        <Border.Triggers>
                            <EventTrigger RoutedEvent="Border.MouseEnter"><BeginStoryboard Storyboard="{StaticResource CardHoverIn}"/></EventTrigger>
                            <EventTrigger RoutedEvent="Border.MouseLeave"><BeginStoryboard Storyboard="{StaticResource CardHoverOut}"/></EventTrigger>
                        </Border.Triggers>
                        <StackPanel>
                            <Border Width="26" Height="26" CornerRadius="6" Background="#ECFEFF" Margin="0,0,0,6">
                                <fa:ImageAwesome Icon="CloudDownload" Width="11" Foreground="#06B6D4" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                            </Border>
                            <TextBlock Text="{Binding TotalSize}" FontSize="18" FontWeight="Bold" Foreground="#0F172A"/>
                            <TextBlock Text="Data Saved" FontSize="10" Foreground="#64748B"/>
                        </StackPanel>
                    </Border>
                </Grid>
                <!-- RECENT DOWNLOADS -->
                <Border Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="14" Margin="0,0,0,14">
                    <Border.Effect><DropShadowEffect BlurRadius="10" ShadowDepth="3" Opacity="0.05"/></Border.Effect>
                    <StackPanel>
                        <Border Padding="16,12,16,10" BorderBrush="#F1F5F9" BorderThickness="0,0,0,1">
                            <Grid>
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <Border Width="26" Height="26" CornerRadius="6" Background="#EFF6FF" Margin="0,0,8,0">
                                        <fa:ImageAwesome Icon="ArrowDown" Width="10" Foreground="#3B82F6" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                    </Border>
                                    <TextBlock Text="Recent Downloads" FontSize="12" FontWeight="SemiBold" Foreground="#0F172A" VerticalAlignment="Center"/>
                                </StackPanel>
                                <TextBlock Text="View All" FontSize="10" FontWeight="SemiBold" Foreground="#3B82F6" Cursor="Hand" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                        <!-- Recent Downloads List -->
                        <ItemsControl ItemsSource="{Binding RecentDownloads}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Padding="16,10,16,12" BorderBrush="#F8FAFC" BorderThickness="0,0,0,1" Cursor="Hand">
                                        <StackPanel>
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="32"/>
                                                    <ColumnDefinition Width="10"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="10"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <Border Grid.Column="0" Width="32" Height="32" CornerRadius="7" Background="#EFF6FF">
                                                    <Image Source="{Binding Filename, Converter={StaticResource FileIconConverter}}"
                                                           Width="20" Height="20" RenderOptions.BitmapScalingMode="HighQuality"
                                                           VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                                </Border>
                                                <StackPanel Grid.Column="2" VerticalAlignment="Center">
                                                    <TextBlock Text="{Binding Filename}" FontSize="11" FontWeight="SemiBold" Foreground="#0F172A" TextTrimming="CharacterEllipsis"/>
                                                    <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                                                        <TextBlock Text="{Binding ProgressDisplay}" FontSize="9" FontWeight="Bold" Foreground="#3B82F6"/>
                                                        <TextBlock Text="  •  " FontSize="9" Foreground="#CBD5E1"/>
                                                        <TextBlock Text="{Binding SizeDisplay}" FontSize="9" Foreground="#94A3B8"/>
                                                        <TextBlock Text="  •  " FontSize="9" Foreground="#CBD5E1"/>
                                                        <TextBlock Text="{Binding Speed}" FontSize="9" Foreground="#64748B"/>
                                                    </StackPanel>
                                                </StackPanel>
                                                <StackPanel Grid.Column="4" Orientation="Horizontal" VerticalAlignment="Center">
                                                    <!-- ACTIVE buttons -->
                                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                                        <StackPanel.Style>
                                                            <Style TargetType="StackPanel">
                                                                <Setter Property="Visibility" Value="Visible"/>
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding State}" Value="Completed">
                                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </StackPanel.Style>
                                                        <Border Width="26" Height="26" CornerRadius="6" Background="#EFF6FF" Cursor="Hand" Margin="0,0,6,0" MouseLeftButtonDown="DashboardPause_Click" Tag="{Binding}">
                                                            <fa:ImageAwesome Width="10" Foreground="#3B82F6" VerticalAlignment="Center" HorizontalAlignment="Center">
                                                                <fa:ImageAwesome.Style>
                                                                    <Style TargetType="fa:ImageAwesome">
                                                                        <Setter Property="Icon" Value="Pause"/>
                                                                        <Style.Triggers>
                                                                            <DataTrigger Binding="{Binding State}" Value="Paused">
                                                                                <Setter Property="Icon" Value="Play"/>
                                                                                <Setter Property="Foreground" Value="#F59E0B"/>
                                                                            </DataTrigger>
                                                                            <DataTrigger Binding="{Binding State}" Value="Failed">
                                                                                <Setter Property="Icon" Value="Refresh"/>
                                                                                <Setter Property="Foreground" Value="#EF4444"/>
                                                                            </DataTrigger>
                                                                        </Style.Triggers>
                                                                    </Style>
                                                                </fa:ImageAwesome.Style>
                                                            </fa:ImageAwesome>
                                                        </Border>
                                                        <Border Width="26" Height="26" CornerRadius="6" Background="#FEF2F2" Cursor="Hand" Margin="0,0,6,0" MouseLeftButtonDown="DashboardCancel_Click" Tag="{Binding}">
                                                            <fa:ImageAwesome Icon="Close" Width="10" Foreground="#EF4444" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                                        </Border>
                                                    </StackPanel>
                                                    <!-- COMPLETED buttons -->
                                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                                        <StackPanel.Style>
                                                            <Style TargetType="StackPanel">
                                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding State}" Value="Completed">
                                                                        <Setter Property="Visibility" Value="Visible"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </StackPanel.Style>
                                                        <Border Width="26" Height="26" CornerRadius="6" Background="#ECFDF5" Cursor="Hand" Margin="0,0,6,0" ToolTip="Open File" MouseLeftButtonDown="DashboardOpenFile_Click" Tag="{Binding}">
                                                            <fa:ImageAwesome Icon="File" Width="10" Foreground="#10B981" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                                        </Border>
                                                        <Border Width="26" Height="26" CornerRadius="6" Background="#EFF6FF" Cursor="Hand" Margin="0,0,6,0" ToolTip="Open Folder" MouseLeftButtonDown="DashboardOpenFolder_Click" Tag="{Binding}">
                                                            <fa:ImageAwesome Icon="FolderOpen" Width="10" Foreground="#3B82F6" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                                        </Border>
                                                        <Border Width="26" Height="26" CornerRadius="6" Background="#F1F5F9" Cursor="Hand" ToolTip="More options">
                                                            <fa:ImageAwesome Icon="EllipsisH" Width="10" Foreground="#64748B" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                                        </Border>
                                                    </StackPanel>
                                                </StackPanel>
                                            </Grid>
                                            <ProgressBar Value="{Binding Progress}" Maximum="100" Style="{StaticResource SlimProgress}" Margin="0,8,0,0"/>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>
                <!-- COMPLETED DOWNLOADS -->
                <Border Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="14">
                    <Border.Effect><DropShadowEffect BlurRadius="10" ShadowDepth="3" Opacity="0.05"/></Border.Effect>
                    <StackPanel>
                        <Border Padding="16,12,16,10" BorderBrush="#F1F5F9" BorderThickness="0,0,0,1">
                            <Grid>
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <Border Width="26" Height="26" CornerRadius="6" Background="#F0FDF4" Margin="0,0,8,0">
                                        <fa:ImageAwesome Icon="CheckCircle" Width="10" Foreground="#22C55E" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                    </Border>
                                    <TextBlock Text="Completed" FontSize="12" FontWeight="SemiBold" Foreground="#0F172A" VerticalAlignment="Center"/>
                                </StackPanel>
                                <TextBlock Text="View All" FontSize="10" FontWeight="SemiBold" Foreground="#22C55E" Cursor="Hand" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                        <!-- Completed Downloads List -->
                        <ItemsControl ItemsSource="{Binding CompletedDownloads}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Padding="16,10,16,12" BorderBrush="#F8FAFC" BorderThickness="0,0,0,1" Cursor="Hand">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="32"/>
                                                <ColumnDefinition Width="10"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="10"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <Border Grid.Column="0" Width="32" Height="32" CornerRadius="7" Background="#DCFCE7">
                                                <Image Source="{Binding Filename, Converter={StaticResource FileIconConverter}}"
                                                       Width="20" Height="20" RenderOptions.BitmapScalingMode="HighQuality"
                                                       VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                            </Border>
                                            <StackPanel Grid.Column="2" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding Filename}" FontSize="11" FontWeight="SemiBold" Foreground="#0F172A" TextTrimming="CharacterEllipsis"/>
                                                <TextBlock Text="{Binding SizeDisplay}" FontSize="9" Foreground="#94A3B8" Margin="0,2,0,0"/>
                                            </StackPanel>
                                            <StackPanel Grid.Column="4" Orientation="Horizontal" VerticalAlignment="Center">
                                                <fa:ImageAwesome Icon="FolderOpen" Width="11" Foreground="#3B82F6" VerticalAlignment="Center" Cursor="Hand" Margin="0,0,10,0"/>
                                                <fa:ImageAwesome Icon="EllipsisH" Width="11" Foreground="#94A3B8" VerticalAlignment="Center" Cursor="Hand"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Border>
</UserControl>
